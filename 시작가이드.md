# puppylovers.club 시작 가이드 🐶🌊📻

이 폴더가 홈 전체입니다. 순서대로 따라오면 사이트가 열려요.

---

## 1. 깃허브 저장소 만들기
1. https://github.com/new 접속 (forj-froms 계정 로그인 상태에서)
2. Repository name: **puppyloversclub**
3. Public 선택 → **Create repository**

> 저장소 이름을 다르게 지으면 blue/index.html, nine/index.html 안의
> `repo:'puppyloversclub'` 부분을 그 이름으로 바꿔야 해요.

## 2. 파일 올리기
1. 만든 저장소에서 **Add file → Upload files**
2. 이 압축을 푼 뒤, 안에 있는 것들을 **전부** 드래그해서 올리기
   (index.html, CNAME, assets 폴더, blue 폴더, nine 폴더 — 폴더째로!)
3. **Commit changes**

## 3. Pages 켜기
1. 저장소 **Settings → Pages**
2. Source: *Deploy from a branch* / Branch: **main**, 폴더 **/(root)** → Save
3. 1~2분 뒤 사이트가 생성됩니다

## 4. 도메인 연결 (jeste.love 때와 같은 과정)
1. 같은 Pages 화면의 **Custom domain**에 `puppylovers.club` 입력 → Save
2. 도메인 산 사이트의 DNS 설정에서 **A 레코드** 4개 추가:
   - 185.199.108.153
   - 185.199.109.153
   - 185.199.110.153
   - 185.199.111.153
3. (선택) `www` 이름으로 **CNAME 레코드** → `forj-froms.github.io`
4. 반영되면(수분~수시간) Pages 화면에서 **Enforce HTTPS** 체크

## 5. 글쓰기 허가증(토큰) 만들기 — 딱 한 번
1. https://github.com/settings/personal-access-tokens → **Generate new token**
2. Token name: 아무거나 (예: puppylovers)
3. Repository access: **Only select repositories → puppyloversclub**
4. Permissions → Repository permissions → **Contents: Read and write**
5. Generate 후 나오는 `github_pat_...` 문자열 복사 (다시 안 보여주니 저장!)

## 6. 사이트에서 글쓰기 ✎
1. puppylovers.club/blue/ (또는 /nine/) 접속
2. 오른쪽 아래 **✎ 버튼 → 설정 탭** → 토큰 붙여넣고 저장 (기기당 1회)
3. **새 글** 탭: 제목·카테고리·본문 쓰고 발행
   - **비밀글** 체크 + 비밀번호 → 글 자체가 암호화되어 올라가요
   - 비밀번호가 길수록 안 뚫립니다 (진짜 숨길 글은 6자 이상 추천)
   - **고정** 체크 → 맨 위 고정글(ANCHORED / NOW BROADCASTING)로
4. **로그 업로드** 탭: 로그 HTML 파일을 고르면 ooc 글로 올라가요
5. **갤러리** 탭: 이미지를 고르면 갤러리에 추가돼요
6. 발행 후 **30초~1분** 뒤 새로고침하면 반영!

## 알아둘 것
- 토큰은 **내 브라우저에만** 저장됩니다. 폰에서도 쓰려면 폰에서 한 번 더 등록.
- 토큰은 비밀번호처럼 취급하세요 (유출되면 저장소에 글을 쓸 수 있어요).
- 방문자에게도 ✎ 버튼은 보이지만, 토큰이 없으면 아무것도 발행할 수 없어요.
- 블루 배경(blue/bg.jpg)·프로필(blue/profile.jpg)은 파일만 갈아끼우면 교체됩니다.
- 랜딩의 LINK 배너 구역은 자리만 잡아둔 상태 — 배너 이미지가 생기면
  Claude에게 가져오면 연결해줄게요.
- 글 수정·삭제 기능은 다음 업데이트에서! (지금은 저장소에서 직접 지울 수 있어요)
